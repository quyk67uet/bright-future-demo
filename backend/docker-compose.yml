version: "3.9"

services:
  redis:
    image: redis:7-alpine
    container_name: bright-future-redis
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    command: ["redis-server", "--appendonly", "yes"]
    restart: unless-stopped

volumes:
  redis-data:
